import { makeStyles } from '@material-ui/core/styles';

export const linkStyles = makeStyles({
    loggedInRoot: {
        backgroundColor: 'black',
        borderBottom: 'solid #FFF500 2px',
    },
    primaryLink: {
        color: 'white',
        fontWeight: 'bold',
    },
});